[project]
name = "ledger"
version = "1.0.0"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
strict_markers = true
xfail_strict = true

[tool.mypy]
strict = true
warn_return_any = true
warn_unused_configs = true